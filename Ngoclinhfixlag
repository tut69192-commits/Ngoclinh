local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "NgocLinhHub"
ScreenGui.Parent = game:GetService("CoreGui")
ScreenGui.ResetOnSpawn = false

local Main = Instance.new("Frame", ScreenGui)
Main.Size = UDim2.new(0,320,0,420)
Main.Position = UDim2.new(0.5,-160,0.5,-210)
Main.BackgroundColor3 = Color3.fromRGB(30,30,30)
Main.BorderSizePixel = 0
Main.Active = true
Main.Draggable = true

Instance.new("UICorner", Main).CornerRadius = UDim.new(0,15)

local Bg = Instance.new("ImageLabel", Main)
Bg.Size = UDim2.new(1,0,1,0)
Bg.BackgroundTransparency = 1
Bg.Image = "rbxassetid://71753856708710"

local Dark = Instance.new("Frame", Main)
Dark.Size = UDim2.new(1,0,1,0)
Dark.BackgroundColor3 = Color3.new(0,0,0)
Dark.BackgroundTransparency = 0.45
Instance.new("UICorner", Dark).CornerRadius = UDim.new(0,15)

local Title = Instance.new("TextLabel", Main)
Title.Size = UDim2.new(1,0,0,40)
Title.BackgroundTransparency = 1
Title.Text = "🌸 Script By Ngọc Linh 🌸"
Title.TextScaled = true
Title.Font = Enum.Font.GothamBold
Title.TextColor3 = Color3.fromRGB(255,182,255)

-- FPS Display
local FpsLabel = Instance.new("TextLabel", Main)
FpsLabel.Size = UDim2.new(0.5,0,0,35)
FpsLabel.Position = UDim2.new(0,5,0,45)
FpsLabel.BackgroundTransparency = 1
FpsLabel.Text = "FPS: 0"
FpsLabel.TextScaled = true
FpsLabel.Font = Enum.Font.GothamBold
FpsLabel.TextColor3 = Color3.fromRGB(0,255,127)

-- Ping Display
local PingLabel = Instance.new("TextLabel", Main)
PingLabel.Size = UDim2.new(0.5,0,0,35)
PingLabel.Position = UDim2.new(0.5,0,0,45)
PingLabel.BackgroundTransparency = 1
PingLabel.Text = "Ping: 0ms"
PingLabel.TextScaled = true
PingLabel.Font = Enum.Font.GothamBold
PingLabel.TextColor3 = Color3.fromRGB(0,200,255)

local Fix = Instance.new("TextButton", Main)
Fix.Size = UDim2.new(0.8,0,0,45)
Fix.Position = UDim2.new(0.1,0,0.75,0)
Fix.Text = "FIX LAG"
Fix.Font = Enum.Font.GothamBold
Fix.TextScaled = true
Fix.BackgroundColor3 = Color3.fromRGB(120,80,255)
Fix.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", Fix)

local Close = Instance.new("TextButton", Main)
Close.Size = UDim2.new(0,30,0,30)
Close.Position = UDim2.new(1,-35,0,5)
Close.Text = "X"
Close.Font = Enum.Font.GothamBold
Close.TextScaled = true
Close.BackgroundColor3 = Color3.fromRGB(220,60,60)
Close.TextColor3 = Color3.new(1,1,1)
Instance.new("UICorner", Close)

-- Nút đóng/mở menu với hình ảnh
local Toggle = Instance.new("ImageButton", ScreenGui)
Toggle.Size = UDim2.new(0,50,0,50)
Toggle.Position = UDim2.new(0.5,-25,0.95,-25)
Toggle.Image = "rbxassetid://71753856708710"
Toggle.BackgroundColor3 = Color3.fromRGB(120,80,255)
Toggle.BackgroundTransparency = 0.2
Toggle.ZIndex = 10
Instance.new("UICorner", Toggle).CornerRadius = UDim.new(0,15)

local isMenuOpen = true

-- Cập nhật FPS
local lastTime = tick()
local frameCount = 0
local fps = 0

game:GetService("RunService").RenderStepped:Connect(function()
    frameCount = frameCount + 1
    local currentTime = tick()
    local deltaTime = currentTime - lastTime
    
    if deltaTime >= 1 then
        fps = frameCount
        frameCount = 0
        lastTime = currentTime
    end
    
    FpsLabel.Text = "FPS: " .. fps
end)

-- Cập nhật Ping
game:GetService("RunService").Heartbeat:Connect(function()
    local stats = game:GetService("Stats")
    local ping = math.floor(stats.Network.ServerStatsItem("Data Ping"):GetValue())
    PingLabel.Text = "Ping: " .. ping .. "ms"
end)

Close.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- Chức năng đóng/mở menu
Toggle.MouseButton1Click:Connect(function()
    isMenuOpen = not isMenuOpen
    Main.Visible = isMenuOpen
    
    if isMenuOpen then
        Toggle.BackgroundColor3 = Color3.fromRGB(120,80,255)
        Toggle.BackgroundTransparency = 0.2
    else
        Toggle.BackgroundColor3 = Color3.fromRGB(100,100,100)
        Toggle.BackgroundTransparency = 0.5
    end
end)

Fix.MouseButton1Click:Connect(function()
    local Lighting = game:GetService("Lighting")

    pcall(function()
        Lighting.GlobalShadows = false
        Lighting.FogEnd = 100000
        Lighting.Brightness = 1
    end)

    for _,v in ipairs(game:GetDescendants()) do
        if v:IsA("ParticleEmitter")
        or v:IsA("Trail")
        or v:IsA("Beam")
        or v:IsA("Smoke")
        or v:IsA("Fire")
        or v:IsA("Sparkles") then
            v.Enabled = false
        elseif v:IsA("BasePart") then
            v.Material = Enum.Material.SmoothPlastic
            v.Reflectance = 0
        end
    end

    Fix.Text = "✓ ĐÃ FIX LAG"
    Fix.BackgroundColor3 = Color3.fromRGB(0,170,127)
end)
